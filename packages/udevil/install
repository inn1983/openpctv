#!/bin/sh

. config/options

mkdir -p $INSTALL/lib/udev/rules.d $INSTALL/etc/udevil
cp $PACKAGES/$1/config/udevil.conf $INSTALL/etc/udevil
cp $ROOT/$PACKAGES/$1/config/*.rules $INSTALL/lib/udev/rules.d/

cd $BUILD/$1*

mkdir -p $INSTALL/usr/bin
cp -PR src/udevil $INSTALL/usr/bin
