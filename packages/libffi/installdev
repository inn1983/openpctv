#!/bin/sh

. config/options

get_meta $1

cd $PKG_BUILD_DIR

cd objdir-target
[ -d .install/usr/lib64 ] && cp -a .install/usr/lib64/* .install/usr/lib/
do_installdev usr/lib*

mkdir .install/usr/include
cp include/* .install/usr/include
do_installdev usr/include
