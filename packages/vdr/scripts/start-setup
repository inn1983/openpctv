#!/bin/bash
export PATH=/usr/local/bin:/usr/local/sbin:/usr/bin:/usr/sbin:/bin:/sbin
export HOME=/root
export TERM=linux

xterm -fg white -bg blue4 -fa 'Droid Sans Fallback:style=Regular' -fs 14 -lc -u8 -maximized -e /sbin/setup

